# Docker Webstack 🚀

A simple Docker Compose setup running a static website with **Nginx** and a **MySQL** database.

---

## Features

- ✅ Static website served by Nginx
- ✅ MySQL 5.7 database
- ✅ Easy to run with Docker Compose
- ✅ Perfect for demos or learning Docker basics

---

## Project Structure

├── docker-compose.yml
└── web
└── index.html


- `docker-compose.yml` → Defines the web and database services.
- `web/index.html` → Your static website content.

---

## Requirements

- Docker
- Docker Compose

---

## Getting Started

1. Clone the repository:
```bash
git clone https://github.com/your-username/docker-webstack.git
cd docker-webstack

start the stack 
docker-compose up -d

open your broweser and visit
http://localhost:9090


